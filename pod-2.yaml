---
apiVersion: v1
kind: Pod
metadata:
  name: pod-2
  namespace: roboshop
spec:
  containers:
    - name: cont-2
      image: nginx
      ports:
        - containerPort: 80
          name: nginx-port
  nodeSelector:
    node: node-2